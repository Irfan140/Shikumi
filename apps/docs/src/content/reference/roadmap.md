# Roadmap

- [ ] Streaming tool output and cancellable runs
- [ ] HTTP MCP transport + auth headers
- [ ] Encrypted BYOK vault (OS keychain)
- [ ] Plugin market for community tools
- [ ] `shikumi` standalone binaries (`bun build --compile`) per platform
- [ ] Eval harness with LangSmith datasets
- [ ] Multi-workspace and remote executor
